-- /etc/dnsdist/dnsdist.conf  --  DNSDist-B1  (10.10.20.16)  -- Site B
----------------------------------------------------------------------
-- Global listeners
----------------------------------------------------------------------
setLocal("10.10.20.16:53")
addLocal("10.10.20.10:53")               -- VIP, active only while MASTER

setACL({"10.10.10.0/24", "10.10.20.0/24", "127.0.0.0/8"})

----------------------------------------------------------------------
-- Packet cache
----------------------------------------------------------------------
pc = newPacketCache(100000, {
    maxTTL = 300,
    minTTL = 10,
    staleTTL = 60,
    temporaryFailureTTL = 30,
    dontAge = false
})
getPool(""):setCache(pc)
getPool("local"):setCache(pc)
getPool("remote"):setCache(pc)

----------------------------------------------------------------------
-- Backend servers
--   pool "local"  = Site B slaves (preferred)
--   pool "remote" = Site A slaves (fallback only)
----------------------------------------------------------------------
newServer({
    address    = "10.10.20.12:53",
    name       = "Knot-B2",
    pool       = "local",
    checkType  = "A",
    checkName  = "_dns-health.domain.local",
    checkInterval = 2,
    maxCheckFailures = 2
})
newServer({
    address    = "10.10.20.13:53",
    name       = "Knot-B3",
    pool       = "local",
    checkType  = "A",
    checkName  = "_dns-health.domain.local",
    checkInterval = 2,
    maxCheckFailures = 2
})
newServer({
    address    = "10.10.10.12:53",
    name       = "Knot-A2",
    pool       = "remote",
    checkType  = "A",
    checkName  = "_dns-health.domain.local",
    checkInterval = 2,
    maxCheckFailures = 2
})
newServer({
    address    = "10.10.10.13:53",
    name       = "Knot-A3",
    pool       = "remote",
    checkType  = "A",
    checkName  = "_dns-health.domain.local",
    checkInterval = 2,
    maxCheckFailures = 2
})

----------------------------------------------------------------------
-- Routing policy: strict local-first, fall back to remote site only
-- when ALL local backends are down. Requires dnsdist >= 1.6.
----------------------------------------------------------------------
setServerPolicy(leastOutstanding)
addAction(PoolAvailableRule("local"), PoolAction("local"))
addAction(AllRule(), PoolAction("remote"))

----------------------------------------------------------------------
-- Security
----------------------------------------------------------------------
addAction(QTypeRule(DNSQType.ANY), RCodeAction(DNSRCode.REFUSED))
addAction(MaxQPSIPRule(1000), DropAction())
addAction(TCPRule(false), MaxQPSRule(5000))
setMaxUDPOutstanding(65535)

----------------------------------------------------------------------
-- Web UI / API - loopback only
----------------------------------------------------------------------
webserver("127.0.0.1:8083")
setWebserverConfig({
    password = "CHANGE_ME_STRONG_PASSWORD",
    apiKey   = "CHANGE_ME_STRONG_API_KEY",
    acl      = "127.0.0.1"
})

----------------------------------------------------------------------
-- Stats / logging
----------------------------------------------------------------------
setRingBuffersSize(500000)
setVerboseHealthChecks(true)
